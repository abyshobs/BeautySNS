@{ Layout = null; }

@model BeautySNS.Admin.Models.StatusUpdates.CreateViewModel

<div>
    <div>

        @using (Html.BeginForm("WriteStatusUpdate", "StatusUpdate", FormMethod.Post))
        {
            @Html.ValidationSummary(true) 
    <div>
        @Html.TextAreaFor(m => m.status, new { placeholder="Write a status update", rows="3", @class = "form-control input-group-lg"})
        @Html.ValidationMessageFor(m => m.status, "", new { @class = "text-danger" })
        <br />
        <button type="submit" class="btn btn-default btn-sm">
            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Post
        </button>
    </div>

        }

    </div>
    </div>


    <br />

  
